# 用户画像标准化跨环境回填执行记录

## 文档定位

- **用途**：作为第六阶段“存量用户画像回填”的正式执行留痕文件。
- **适用范围**：仅覆盖 `user_profiles.degree / major` 与 `user_job_preference_tags.intentionCity / intentionJob / intentionCompany` 的标准化回填。
- **配套文档**：`docs/用户画像与求职意向标准化回填说明.md`、`docs/旧canonical到新canonical映射表.md`。

## 当前执行状态总览

| 环境 | 状态 | 环境文件 / 连接方式 | 执行命令 | 当前结论 |
| --- | --- | --- | --- | --- |
| 开发环境 | 已完成 | `.env` + 本地 Docker API 容器 | `npm run db:backfill:user-normalization -- --apply --userId=4b77d514-b5b1-424c-91e2-0e1f047e6d5d` | 已完成单用户正式回填、dry-run 复核归零、API 缓存刷新 |
| 测试环境 | 待执行 | **当前仓库未见独立测试环境连接信息** | 待维护窗口执行 | 已补充执行模板与核验清单，待拿到环境访问方式后落地 |
| 生产环境 | 待执行 | **当前仓库未见独立生产环境连接信息** | 待维护窗口执行 | 已补充执行模板与核验清单，待生产窗口和审批后落地 |

## 已完成记录：开发环境

### 执行口径

- **执行日期**：2026-04-29
- **执行入口**：根脚本 `db:backfill:user-normalization`
- **根脚本现口径**：`npm run prisma:backfill-user-normalization --workspace @offer360/api --`
- **关键修复**：已修正根脚本参数透传问题，确保 `--apply / --userId` 能传到 `apps/api/prisma/backfill-user-normalization.ts`

### 实际动作

1. 显式加载开发环境变量文件 `.env`
2. 执行正式写库：
   - `npm run db:backfill:user-normalization -- --apply --userId=4b77d514-b5b1-424c-91e2-0e1f047e6d5d`
3. 再次执行 dry-run 复核：
   - `profile_changes=0`
   - `preference_changes=0`
4. 重启开发 API 容器刷新进程内标准化缓存与推荐缓存：
   - `docker compose --env-file .env -f docker-compose.dev.yml restart api`

### 开发环境结论

- 指定开发库用户画像与求职意向已正式回填到当前短 canonical 口径
- 回填后二次 dry-run 已归零，说明当前开发库该执行对象已无残余待迁移数据
- API 缓存已刷新，避免“数据库已更新但运行中缓存仍是旧口径”的假象

## 测试 / 生产环境执行模板

### 执行前检查

- 已确认目标环境数据库备份或快照可回滚
- 已确认目标环境 API 重启窗口
- 已确认本次回填仅影响用户画像与求职意向，不触碰招聘公告原文
- 已准备好本轮正式映射依据：`docs/旧canonical到新canonical映射表.md`

### 推荐执行顺序

1. 先 dry-run，记录变更数量与前 5 条样例
2. 先用 `--userId=` 做 1~3 个测试账号灰度验证
3. 验证个人中心、后台用户页、专属推荐是否一致
4. 再执行全量 `--apply`
5. 回填完成后重启实际承载 API 的进程或容器
6. 再做一次 dry-run，确认变化归零或下降到预期范围

### 测试环境执行留痕模板

| 字段 | 填写内容 |
| --- | --- |
| 执行日期 | 待填写 |
| 执行人 | 待填写 |
| 环境标识 | 测试环境 |
| 环境文件 / 连接方式 | 待填写 |
| dry-run 命令 | `npm run db:backfill:user-normalization` |
| dry-run 结果 | 待填写 |
| 灰度 apply 命令 | `npm run db:backfill:user-normalization -- --apply --userId=<测试用户ID>` |
| 灰度验证结果 | 待填写 |
| 全量 apply 命令 | `npm run db:backfill:user-normalization -- --apply` |
| 全量执行结果 | 待填写 |
| API 刷新动作 | 待填写 |
| 回填后 dry-run 结果 | 待填写 |
| 页面复核结论 | 待填写 |

### 生产环境执行留痕模板

| 字段 | 填写内容 |
| --- | --- |
| 执行日期 | 待填写 |
| 执行人 | 待填写 |
| 环境标识 | 生产环境 |
| 审批 / 变更单 | 待填写 |
| 环境文件 / 连接方式 | 待填写 |
| dry-run 结果 | 待填写 |
| 灰度 apply 用户 | 待填写 |
| 灰度验证结果 | 待填写 |
| 全量 apply 结果 | 待填写 |
| API / 容器刷新动作 | 待填写 |
| 回填后 dry-run 结果 | 待填写 |
| 用户抽样复核结论 | 待填写 |
| 异常与回滚情况 | 待填写 |

## 当前未闭环原因说明

- **当前仓库内可验证的实际执行只有开发环境**；测试环境、生产环境的数据库连接信息、维护窗口和审批链路未随仓库提供。
- 因此，本文件本次先完成了：
  - 开发环境正式执行记录固化
  - 测试 / 生产环境的标准留痕模板补齐
  - 执行前后检查项与命令模板固化
- 后续一旦拿到测试 / 生产环境访问方式，只需按本文件继续补录即可，不再需要重新整理执行口径。
