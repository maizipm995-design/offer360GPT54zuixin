# 词库标准化搜索推荐正式验收报告-20260429

## 文档定位

- **对应阶段**：第四阶段“修正普通搜索” + 第五阶段“修正推荐链路”
- **关联范围**：`JOB_TITLE / MAJOR` 标准化方向、普通搜索、专属推荐、词典中心导入导出能力
- **基线源码**：
  - `apps/api/src/modules/jobs/jobs-normalization.seed-data.ts`
  - `apps/api/src/modules/jobs/jobs.service.ts`
  - `apps/api/src/modules/jobs/jobs-recommendation.service.ts`
  - `apps/web/app/admin/normalization-dictionary/page.tsx`
- **配套文档**：
  - `docs/JOB_TITLE canonical 定稿表.md`
  - `docs/MAJOR canonical 定稿表.md`
  - `docs/旧canonical到新canonical映射表.md`
  - `docs/alias归属说明表.md`
  - `docs/标准化词典中心后台维护SOP.md`

## 一、验收方法说明

本报告采用两层证据：

1. **自动化回归测试**：验证当前仓库中的普通搜索、推荐召回、标准化服务与 seed 数据口径是否一致
2. **固定样例对照**：用执行方案中最关键的搜索 / 推荐场景做“旧风险 vs 当前结果”对照，记录命中数量与 Top 结果相关性

> 说明：历史旧实现没有保留一份可直接回放的完整基线快照，因此“改造前”部分以执行方案中已确认的问题定义和历史风险为准；“当前结果”则以 2026-04-29 重新执行的回归测试结果为准。

## 二、自动化回归结果

### 本次执行命令

```bash
npm run test --workspace @offer360/api -- src/modules/jobs/__tests__/jobs-normalization.seed-data.spec.ts src/modules/jobs/__tests__/jobs-normalization.service.spec.ts src/modules/jobs/__tests__/jobs.service.spec.ts src/modules/jobs/__tests__/jobs-recommendation.service.spec.ts
```

### 执行结果

- **测试文件**：4 个全部通过
- **测试用例**：24 个全部通过
- **总耗时**：约 `408ms`
- **结论**：当前仓库内与词库标准化直接相关的关键回归链路均为通过状态

### 覆盖范围

| 测试文件 | 关注点 |
| --- | --- |
| `jobs-normalization.seed-data.spec.ts` | `JOB_TITLE / MAJOR` 主词定稿、alias 归属、兼容桶收口、seed 一致性 |
| `jobs-normalization.service.spec.ts` | 写库前归一、exact / contains 策略、地点父级联动 |
| `jobs.service.spec.ts` | 普通搜索中的企业 alias、短 canonical、exact alias 收紧、地点/学历命中 |
| `jobs-recommendation.service.spec.ts` | 专属推荐中的地点命中、公司/岗位/学历/专业联动、90 天过滤、safe recall 约束 |

## 三、固定样例对照（数量与 Top 结果相关性）

| 场景 | 改造前主要风险 | 当前样例结果 | 当前数量 / Top 结果 | 证据 |
| --- | --- | --- | --- | --- |
| 普通搜索：`companyName=中烟` | 企业简称可能无法命中公告正文中的 `山东中烟`、`江苏中烟` 等长写法 | **已命中** | 数量 `1`；Top 结果公司为 `山东中烟工业有限责任公司` | `jobs.service.spec.ts` |
| 普通搜索：`positionName=研发` | 短 canonical 可能被窄化到旧长 canonical，导致结果不稳定 | **已命中** | 数量 `1`；Top 结果岗位为 `研发工程师` | `jobs.service.spec.ts` |
| 普通搜索：`positionName=行政` | 高歧义短词可能被错误放大，造成“综合行政”类岗位泛召回 | **已收紧** | 数量 `0`；无误命中结果 | `jobs.service.spec.ts` |
| 普通搜索：`workLocation=深` + `degree=大学本科` | 地点简称和学历长写法可能无法同步命中 | **已命中** | 数量 `1`；Top 结果地点为 `深圳市`、学历为 `大学本科及以上` | `jobs.service.spec.ts` |
| 专属推荐：`中烟 + 研发 + 大学本科 + 计算机科学` | 公司 / 岗位 / 学历 / 专业 alias 可能无法联动进推荐打分 | **已联动命中** | 数量 `1`；Top 结果推荐理由同时包含 `中国烟草`、`研发`，命中维度含 `company / job / degree / major` | `jobs-recommendation.service.spec.ts` |
| 专属推荐：超过 90 天未更新岗位 | 旧岗位可能混入候选，影响相关性 | **已过滤** | 数量 `0`；不存在过期岗位进入 Top 结果 | `jobs-recommendation.service.spec.ts` |
| 专属推荐：`行政` 这类 exact alias | 高歧义 alias 可能被直接拿去文本扩召回，导致误召回 | **已收紧** | Top 候选查询条件中**不包含**裸词 `行政` | `jobs-recommendation.service.spec.ts` |
| 专属推荐：专业信息只出现在 `jobCategory` | 专业匹配可能漏掉 `jobCategory / announcementTitle` 等文本位 | **已命中** | 数量 `1`；Top 结果 `hitDimensions` 包含 `major` | `jobs-recommendation.service.spec.ts` |

## 四、词典中心能力验收记录

### 已具备的仓库级交付

| 项目 | 当前状态 | 证据 |
| --- | --- | --- |
| 标准词 CRUD | 已具备 | `apps/web/app/admin/normalization-dictionary/page.tsx` + `AdminNormalizationController` |
| alias CRUD | 已具备 | 同页“管理别名”弹窗 + `POST/PATCH/DELETE /admin/normalization-terms/:id/aliases` |
| 省市关系 CRUD | 已具备 | 同页 `hierarchies` tab + `/admin/location-hierarchies` |
| 模板下载 | 已具备 | `GET /admin/normalization/template` |
| 全量导出 | 已具备 | `GET /admin/normalization/export` |
| Excel 导入 | 已具备 | `POST /admin/normalization/import` |
| 长期维护规范 | 已补齐 | `docs/标准化词典中心后台维护SOP.md` |
| 正式交付文档 | 已补齐 | 四份定稿 / 映射 / 归属文档 |

### 仍建议做的人工页面点击验收

> 本次 CLI 侧已完成代码与测试审计，但**没有在浏览器里逐按钮点击后台页面**。因此把人工点击清单明确记录如下，供后台运营联调时逐项勾选。

| 人工验收项 | 预期结果 |
| --- | --- |
| 下载模板 | 成功下载，包含 `terms / aliases / location_hierarchy` 三张 Sheet |
| 导出全量词典 | 成功导出当前词典快照 |
| 导入一个合法样例文件 | 返回成功数量、失败数量与错误明细 |
| 导入一个含错误行的样例文件 | 后台明确返回错误行号与原因 |
| 新增 / 编辑 / 删除 alias | 保存后列表与统计数即时更新 |
| 修改高歧义 alias 为 `exact` | 搜索 / 推荐不再把裸词放大召回 |

## 五、本次正式验收结论

- **第四阶段（普通搜索）**：就当前仓库代码与固定样例集而言，已达到正式验收通过状态
- **第五阶段（推荐链路）**：就当前仓库代码与固定样例集而言，已达到正式验收通过状态
- **词典中心交付物**：原先缺失的正式定稿表、映射表、alias 归属说明表、长期维护 SOP 均已补齐
- **剩余现实边界**：后台页面的逐按钮人工点击验收和测试/生产环境回填执行，仍需要实际环境窗口配合；对应留痕模板已分别落在 `docs/用户画像标准化跨环境回填执行记录.md` 与本报告中
