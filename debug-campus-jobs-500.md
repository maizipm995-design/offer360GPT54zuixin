[OPEN] campus-jobs-500

# Debug Session: campus-jobs-500

## Symptom
- 生产环境中，“名企校招”页面的“免费专区、全部招聘、专属推荐”栏目内，点击“查看公告”或“立即投递”时常返回 500：
- `{"statusCode":500,"message":"Internal server error"}`
- 本地预览未复现。

## Scope
- 前端：名企校招页面及其点击链路
- 前端代理：`apps/web/app/api/proxy/[...path]/route.ts`
- 后端：jobs 模块及直接关联查询/跳转逻辑
- 运行环境：生产 `api` / `web` 容器日志与线上接口响应

## Hypotheses
1. 线上存在脏数据或缺字段记录，点击特定公告时后端在组装响应时抛错。
2. 线上数据库结构/数据与本地不一致，jobs 查询或映射逻辑命中异常字段。
3. 前端某些入口传参为空、缺失或格式异常，生产环境下未被后端兜底。
4. 线上环境变量或依赖配置缺失，仅在公告详情/投递链路触发 500。

## Evidence Log
- 已检查生产 `web` 容器日志中的 jobs 点击链路，当前观测到的状态码为 `201 / 429 / 403`，暂未抓到 `500`。
- 已用临时测试账号 `13988683002` 验证“全部招聘”第一页前 20 条的 `view-announcement / deliver` 签发链路，未出现 `500`。
- 同一账号继续扫描后触发 jobs 风控限流，返回 `429`，说明不能用单账号高频盲扫整站样本。
- 已用第二个临时测试账号 `13900035778` 复测“全部招聘”第一页，仍未复现 `500`。
- 当前缺少一个“用户实际点击即可复现”的具体岗位样本（公司名 / 岗位标题 / 所在栏目 / 大概时间）。

## Status
- 已完成首轮线上证据采集，但暂未稳定复现 `500`；下一步需要用户提供具体失败样本，或在锁定样本后补最小化埋点。
