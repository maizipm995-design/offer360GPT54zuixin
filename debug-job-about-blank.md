# [OPEN] job-about-blank

## 背景
- 症状：免费专区中“一汽丰田”岗位，点击“查看公告”或“立即投递”后，新窗口显示 `about:blank`
- 期望：应跳转到真实公告页或投递页

## 假设
- H1：前端 `window.open('', '_blank', ...)` 成功打开了空白页，但后续 `location.href` 赋值失败或未执行
- H2：前端拿到的 `redirectPath` 为空、非法，或被归一化后仍是不可导航地址
- H3：Web 端代理 `/api/proxy/...` 或 API 重定向接口返回了浏览器无法直接打开的响应
- H4：`noopener,noreferrer` 组合导致返回的窗口句柄不可用，空白页被打开后无法再被脚本重定向
- H5：目标接口返回 30x/40x 后被浏览器或新窗口拦截，最终停留在 `about:blank`

## 计划
- 第一步：在招聘页按钮点击链路增加调试埋点，记录 `pendingWindow`、`redirectPath`、归一化结果、实际赋值动作
- 第二步：必要时补充 Web 代理层埋点，观察请求与响应状态
- 第三步：根据证据做最小修复

## 进展
- 2026-05-26：创建调试会话，等待植入埋点并复现
