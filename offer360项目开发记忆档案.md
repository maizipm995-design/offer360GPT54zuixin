# offer360项目开发记忆档案
> 版本：v1.7 | 更新时间：2026-06-03 | 状态：开发中 | 唯一官方交接文档

---

## 一、项目基本信息
### 1. 规范项目名称
**offer360校招求职服务平台**

### 2. 项目简介与核心定位
面向应届毕业生的一站式校招服务平台，聚合全国校招岗位信息，提供求职服务商品、会员体系、邀请分销、后台运营管理能力，支持B端运营和C端用户全链路使用。

### 3. 整体技术栈
| 层级 | 技术选型 | 版本要求 |
|------|----------|----------|
| 前端 | Next.js 14 + React 18 + TypeScript + Tailwind CSS + Zustand + shadcn/ui | Node.js >= 20.18.0 |
| 后端 | NestJS 10 + TypeScript + Prisma ORM + JWT鉴权 + MySQL | Node.js >= 20.18.0 |
| 数据库 | MySQL 8.0 + Redis（预留） + Elasticsearch（预留） | MySQL 8.0+ |
| 部署 | Docker Compose + Monorepo 多包结构（apps/api + apps/web + packages/shared） | Docker 24.0+ |
| 构建工具 | npm + TypeScript + ESLint | npm >= 11.12.1 |

---

## 二、18轮开发关键历程复盘
### 第1轮：需求确认与方案授权
- **用户需求**：确认采用方案C落地，授权开发端全权操作数据库，允许新建兑换码/管理员/核销表、旧表扩展字段
- **核心动作**：确认需求范围，创建待办清单，梳理项目结构
- **产出**：方案C正式确认函，开发范围边界明确

### 第2轮：数据库结构规划
- **核心动作**：梳理现有MVP库结构，对齐方案C需要新增的表和扩展字段
- **产出**：Prisma schema 改造清单，包含管理员体系、兑换码体系、业务表扩展字段

### 第3轮：数据库模型落地
- **核心动作**：扩展Prisma schema，新增`admin_users`/`admin_roles`/`admin_user_roles`/`admin_role_permissions`/`admin_operation_logs`/`membership_redeem_code_batches`/`membership_redeem_codes`/`membership_redeem_use_logs`表，为`users`/`job_announcements`/`user_memberships`/`membership_rich_text_contents`/`service_orders`新增扩展字段
- **产出**：方案C数据库模型正式写入代码

### 第4轮：业务链路基础改造
- **核心动作**：实现会员兑换码核心业务逻辑，新增`/me/membership/redeem`接口，前台会员页接入真实兑换表单，岗位查询默认只返回`published`状态数据，避免草稿透出
- **产出**：用户侧兑换码链路打通，前台数据安全收敛完成

### 第5轮：后台接口适配扩展
- **核心动作**：后台用户/招聘公告接口支持新扩展字段（用户状态/来源、公告状态/来源/备注），列表新增对应筛选能力
- **产出**：后台CRUD接口与新字段适配完成

### 第6轮：数据库兼容性调整
- **问题**：开发库对新表挂旧表外键存在兼容性冲突
- **核心动作**：保留追踪字段与索引，移除跨旧表强外键约束，保证结构可正常推送
- **产出**：数据库结构成功推送到本地开发库，无兼容性错误

### 第7轮：演示数据灌入
- **核心动作**：更新seed脚本，写入默认超级管理员（账号：offer360_admin / 密码：Admin@Offer360#2026）、角色权限、演示兑换码批次（RC20260425A）、4个测试兑换码（OF360-MONTH-0001~0004）
- **产出**：开发库演示数据完整，可直接验证功能

### 第8轮：全量构建验证
- **核心动作**：执行整仓构建，修复类型错误与环境变量污染问题，确认前后端代码均可正常编译
- **产出**：整仓构建全绿，代码层无语法错误

### 第9轮：预览环境验证
- **核心动作**：重建前后端开发容器，校验会员页兑换码入口可正常访问，兑换功能可实际使用
- **产出**：开发环境可正常运行，核心业务链路可用

### 第10轮：后台认证体系开发
- **核心动作**：新增管理员专用JWT策略、登录守卫、权限装饰器、登录/会话接口，实现管理员与普通用户登录态隔离
- **产出**：后台认证底座完成，支持角色与权限校验

### 第11轮：批量导入导出能力开发
- **核心动作**：新增CSV解析/生成工具，实现招聘公告/用户的模板下载、导出、批量导入接口，支持批量写入数据库
- **产出**：批量运营能力后端接口完成

### 第12轮：兑换码后台服务开发
- **核心动作**：新增兑换码批次管理、明细管理、作废/恢复接口，支持按批次生成兑换码、批量调整有效期、查询使用记录
- **产出**：兑换码后台服务能力完成

### 第13轮：后台主控制器全量鉴权
- **核心动作**：为所有现有后台接口统一接入管理员登录守卫和权限声明，未登录/无权限无法访问后台接口
- **产出**：后台接口安全收敛完成，裸奔问题修复

### 第14轮：前端后台登录体系开发
- **核心动作**：新增后台登录页，实现管理员cookie存储、前端代理自动注入Authorization头、会话过期自动跳转登录
- **产出**：前端后台登录链路完成

### 第15轮：后台壳权限控制开发
- **核心动作**：重写AdminShell，实现管理员会话校验、按权限过滤侧边栏菜单、管理员信息展示、退出登录功能
- **产出**：后台菜单权限控制完成，无权限菜单自动隐藏

### 第16轮：前后端类型同步扩展
- **核心动作**：同步扩展前后端类型定义，纳入新增字段与新模块（兑换码、管理员会话、导入导出）类型
- **产出**：前后端类型对齐，无类型错误

### 第17轮：核心能力验收
- **用户需求**：继续开发后台管理员登录、角色权限接入、兑换码后台两页、招聘公告/用户批量导入导出与模板下载
- **核心动作**：完成上述需求的后端服务与前端骨架开发，进入页面落地阶段
- **产出**：核心能力后端全部完成，前端页面待落地

### 第18轮：简历编辑器 OSS 上传故障修复
- **问题**：简历编辑器上传校徽时出现 `XHR error PUT` 报错，原因为 `resume-editor-page-client.tsx` 中 local 的 `uploadFileToOss` 未支持 CNAME 模式，导致使用自定义域名时 URL 拼接错误（Bucket 被错误地前置到 CNAME 域名前）。
- **核心动作**：
  1. 移除 `resume-editor-page-client.tsx` 中的冗余 `uploadFileToOss` 本地实现及 `ali-oss` 依赖。
  2. 统一调用 `lib/oss.ts` 中的共享上传逻辑。
  3. 优化 `lib/oss.ts` 中的 `shouldUseOssCname` 函数，支持更鲁棒的 CNAME 自动识别逻辑（非 `*.aliyuncs.com` 均视为 CNAME）。
- **产出**：解决了简历编辑器图片上传故障，提升了 OSS 上传组件的复用性与健壮性。

### 第19轮：云服务器数据库参考数据同步（标准化词典 + 服务商品）
- **用户需求**：检查标准化词典数据表与求职服务商品数据表，并统一同步到云服务器数据库；商品实际售价为 499/599/699 等，原价为实际售价的 130% 向上取整。
- **核心动作**：
  1. 新增仅同步“标准化词典（normalization_terms / normalization_aliases / location_hierarchies）+ 服务商品（service_products）”的脚本 `apps/api/prisma/sync-reference-tables.ts`，避免使用 `seed.ts` 导致演示账号/示例招聘公告污染线上库。
  2. 同步时强制按 `Math.ceil(price * 1.3)` 计算 originalPrice，保证价格逻辑一致。
  3. 在云服务器上复用正在运行的 API 容器环境执行同步脚本（避免因缺失 .env 导致 DATABASE_URL 不一致）。
- **产出**：云服务器数据库已完成参考数据同步；service_products 价格与 original_price 计算规则校验通过。
  - **补充**：用户目标为“一次性同步”，已移除便捷 npm script 入口，并为脚本增加显式确认开关（需设置 `CONFIRM_SYNC_REFERENCE_TABLES=YES` 才能运行），防止误触发重复写入。

### 第20轮：简历优化“全文一键优化”改造为异步任务模式
- **用户需求**：简历优化页面的“全文一键优化”不能继续让前端长时间等待，需要改成异步任务提交 + 状态轮询模式。
- **核心动作**：
  1. 复用 `resume_ai_optimization_logs` 作为任务持久化载体，不新增独立任务表；`POST /me/resume-drafts/:id/ai-optimize` 改为仅提交任务，新增 `GET /me/resume-drafts/:id/ai-optimize/tasks/:taskId` 查询状态。
  2. 后端将全文优化执行从请求链路中剥离，改为后台任务执行，并增加超时失效处理，避免旧 `processing` 任务卡死。
  3. 为防止异步结果覆盖用户在任务期间的新修改，全文优化落库时按字段做冲突检查，只对“提交任务时未被再次修改”的字段应用 AI 结果。
  4. 前端按钮改为“提交任务后轮询完成状态”，任务完成后自动刷新简历；全局 AI 撤回不再沿用旧同步逻辑，避免错误回滚异步期间的新内容。
- **产出**：全文 AI 优化已从同步等待改为异步任务模式，显著降低前端阻塞风险，并补上异步场景下的覆盖冲突保护。

### 第21轮：面试逐字稿页面改造成异步任务 + 生成记录模式
- **用户需求**：面试逐字稿页面需支持全局导航入口、结构化简历/本地附件二选一提交、调用外部工作流生成 Word 下载结果，并在“生成记录”中持续查看状态与下载入口。
- **核心动作**：
  1. 在 `apps/api` 新增 `InterviewTranscriptTask` 持久化模型与迁移文件，任务表仅保存公司/岗位/面试类型、简历来源方式、生成状态、下载结果，不保存简历正文或附件内容。
  2. 新增 `interview-transcripts` Nest 模块，将工作流执行从前端同步等待改为“提交任务即返回 + 服务端后台异步处理 + 轮询查询状态”；其中“通用综合面试”在服务端自动映射为工作流要求的 `通用综合面`。
  3. 本地上传附件继续采用临时文件直传工作流方式，生成完成或失败后立即删除临时文件；结构化简历 JSON 仅在内存中透传，不落库。
  4. 前端 `interview-transcript-page-client.tsx` 改为调用新 API：提交后本地仅保存任务 ID，页面重新进入后通过任务 ID 批量查询生成记录，并对“生成中”任务自动轮询刷新状态。
  5. 清理旧的 Next 同步工作流路由实现，避免两套逐字稿链路并存导致行为分叉。
- **产出**：面试逐字稿功能已具备真实异步任务能力，符合“提交后 5-10 分钟在生成记录查看结果”的业务形态，同时满足“简历数据只用于本次生成、完成即清理”的约束。

---

## 三、已完成功能模块
### 1. 前台用户端
✅ 岗位列表/详情/投递/进度更新/内推码获取  
✅ 用户注册/登录/个人中心/资料修改/意向设置  
✅ 会员开通/兑换码兑换/会员权益展示  
✅ 服务商品列表/详情/下单（未接支付）  
✅ 邀请体系/分销关系绑定/佣金计算（未接提现）  
✅ 前后端代理与接口统一封装

### 2. 后台运营端
✅ 管理员登录/会话管理/权限校验/角色管理（数据层）  
✅ 数据总览/招聘公告管理/用户管理/会员管理/会员内容管理  
✅ 服务商品管理/订单管理/分销流水管理/分销配置  
✅ 兑换码批次管理/明细管理/作废恢复（后端接口）  
✅ 招聘公告/用户模板下载/导出/批量导入（后端接口）

### 3. 底层能力
✅ JWT鉴权/用户与管理员登录态隔离/权限控制  
✅ MySQL数据库结构/表关系/索引设计  
✅ Docker Compose 开发部署环境  
✅ Prisma 客户端生成与数据库迁移工具链  
✅ CSV导入导出/兑换码生成/核销逻辑

---

## 四、当前项目开发进度&代码现状
### 1. 开发进度
- 核心业务链路：90%完成（仅剩支付/消息/运营工具前端）
- 后台运营体系：70%完成（仅剩兑换码页面、导入前端、权限精细化）
- 基础设施：100%完成（部署/构建/数据库/鉴权/日志链路）

### 2. 代码现状
- 目录结构：标准Monorepo，`apps/api`为NestJS后端，`apps/web`为Next.js前端，`packages/shared`为公共类型
- 数据库：已推送至开发库，所有表结构完整，演示数据已灌入
- 构建状态：整仓构建全绿，无语法/类型错误
- 运行状态：开发环境可正常启动，前台/后台可正常访问，核心接口可正常调用

---

## 五、未完成待开发功能
### 1. 高优先级
1. 兑换码批次管理前端页面（`/admin/redeem-batches`）
2. 兑换码明细管理前端页面（`/admin/redeem-codes`）
3. 招聘公告/用户批量导入前端上传入口
4. 招聘公告/用户全量导出能力（当前仅支持导出当前页）
5. 后台管理员操作日志落地与查询
6. 角色权限精细化配置页面

### 2. 中优先级
1. 订单支付链路接入（微信/支付宝）
2. 用户消息推送（短信/邮件/站内信）
3. 岗位爬虫与自动更新
4. 会员到期/订单状态通知
5. 分销佣金提现功能
6. 站内搜索与Elasticsearch接入

### 3. 低优先级
1. 移动端H5适配优化
2. SEO优化与服务端渲染落地
3. 多环境配置（测试/预发/生产）
4. 监控与告警接入
5. 多语言支持

---

## 六、现有代码规范
### 1. 目录规范
```
apps/api/src
├── modules/        # 业务模块目录，每个模块独立Controller/Service/DTO
├── common/         # 公共工具/守卫/装饰器/类型
├── prisma.service.ts # Prisma统一实例
└── main.ts         # 应用入口

apps/web/app
├── admin/          # 后台页面
├── (user)/         # 前台页面
├── api/proxy/      # 前端代理路由
└── layout.tsx      # 全局布局

apps/web/components
├── admin/          # 后台通用组件
├── ui/             # shadcn/ui基础组件
└── layout/         # 前台布局组件
```

### 2. 命名规范
- 接口路径：后台接口统一前缀`/admin`，用户接口统一前缀`/me`，公共接口无特殊前缀
- 数据库表名：蛇形命名，复数形式，如`job_announcements`/`admin_users`
- 代码命名：驼峰命名，类型/类大驼峰，变量/函数小驼峰，常量全大写下划线分隔
- 权限命名：模块:操作:范围，如`admin:job:manage`/`dashboard:view`

### 3. 接口规范
- 所有接口统一返回格式：`{ code: 0, data: T, message?: string }`
- 错误码：401未登录/403无权限/404不存在/400参数错误/500服务错误
- 分页接口统一返回格式：`{ list: T[], pagination: { page: number, limit: number, total: number, hasMore: boolean } }`

### 4. 数据库规范
- 所有表必须包含`id`/`created_at`/`updated_at`字段
- 关联字段统一用`_id`后缀，如`user_id`/`batch_id`
- 扩展字段优先新增，禁止修改已有字段类型/名称
- 索引必须添加，禁止全表扫描查询

---

## 七、已知问题&限制
1. **外键限制**：新管理员/兑换码表与旧用户/会员表无强外键约束，仅保留ID字段用于追踪，业务层自行保证数据一致性
2. **导出限制**：当前导出接口最多返回1000条，全量导出需分页处理
3. **导入限制**：单次导入最多支持5000条，超过需拆分文件
4. **权限限制**：当前仅支持超级管理员角色，精细化角色权限需后续开发
5. **操作日志**：管理员操作日志表结构已定义，但尚未落地日志写入逻辑
6. **支付链路**：服务订单仅完成结构与后台管理，未接入实际支付通道
7. **权限泄漏风险**：后台接口仅校验管理员登录，前台`/jobs`接口未做频率限制，后续需添加限流

---

## 八、后续开发约束与注意事项
1. **权限规则**：新增后台接口必须加`@UseGuards(AdminJwtAuthGuard, AdminPermissionGuard)`和`@RequireAdminPermissions`装饰器，禁止裸奔接口
2. **数据安全**：前台接口永远不返回未发布/草稿数据，敏感字段（如密码hash）永远不返回给前端
3. **数据库变更**：表结构变更必须走Prisma migrate，禁止直接修改数据库
4. **兼容性规则**：修改已有接口必须向前兼容，禁止修改已有返回字段结构
5. **兑换码规则**：兑换码状态变更必须记录操作人，已使用/过期兑换码不可再次使用
6. **支付规则**：订单支付成功后必须校验订单状态，避免重复支付，必须记录支付回调日志
7. **交接规则**：后续开发必须以本记忆档案为唯一权威依据，历史对话仅作辅助参考，修改本档案需同步更新版本号
8. **AI配置规则**：后台`ai_model_configs`当前仅有1条默认启用配置`resume_optimizer_default`；运行时真实消费的提示词字段只有`systemPrompt`、`globalPromptTemplate`、`entryPromptTemplate`。其中`globalPromptTemplate`并非只用于“全局优化”，还同时服务`resume_section_optimize`与`resume_translate`；`remark`仅用于后台说明，不参与模型调用，允许作为配置用途说明补全
9. **AI优化规则**：局部条目优化与单字段优化禁止再用“内容过短不优化”拦截；短内容必须优先做基于事实的扩写与润色。优化时优先使用用户求职意向，若未填写则从经历`roleName`、项目角色、校内角色、专业等直接信号推断优化方向；AI请求禁止传输手机号、邮箱，且全局优化需压缩空字段、提高`maxOutputTokens`与容错，避免模型返回截断导致前端长时间转圈
10. **AI会员与撤回规则**：普通用户不可用任何AI优化；标准会员可用“全文一键AI优化”但不可用单段/二次深度优化；超级会员可用全部。任意AI优化完成后必须提供“撤回还原”，一键恢复到优化前内容。AI评估建议链路与实际优化链路必须彻底拆分：保存内容后自动生成2-3条建议（免费）；点击建议触发二次深度优化时再做会员校验，并把建议方向传给后端作为selectedSuggestion参与优化
11. **2026-05-17 生产部署记忆**：生产服务器`175.24.133.232`当前发布标签已更新为`20260517-171703`，发布仍走`/opt/offer360/deploy/prod/scripts/deploy-release.sh`离线包模式；宝塔面板旧入口`/6e6e0272`已失效，实际执行应优先走仓库内现成 PEM 密钥的`ssh/scp`通道
12. **2026-05-17 定向数据同步规则**：本次仅同步`ai_model_configs`与`service_products`两张表，线上同步前应先在`/opt/offer360/deploy/prod/runtime/manual-sync-backups`生成目标表备份；线上当前校验结果为`ai_model_configs=1`、`service_products=9`
13. **2026-05-17 导库坑点**：跨环境导出这两张表时，禁止使用不带列名的`INSERT/REPLACE ... VALUES (...)`，否则在字段顺序不一致时会写坏`ai_model_configs.professional_prompt_template`与`assessment_prompt_template`；必须使用`mysqldump --complete-insert --replace`或手写带列名 SQL
14. **2026-05-17 展示校验记忆**：公网`https://www.offer360.cn`已正常切到新标签；`/api/proxy/service-products`当前返回7条`productType=service`商品，详情接口如`service-background-training`、`service-written-test-companion`可正常返回非空`detailHtml`
15. **2026-05-18 标准化词典同步记忆**：标准化词典中心当前生产同步范围只包含`normalization_terms`、`normalization_aliases`、`location_hierarchies`三张表，不包含`service_products`等其他参考数据；本地开发库当前基线为`terms=411`、`aliases=478`、`hierarchies=289`
16. **2026-05-18 标准化词典验收口径**：同步后不能只看总数，必须同时校验`normalization_terms`按`domain`分布一致；当前基线为`COMPANY=23`、`DEGREE=5`、`JOB_TITLE=39`、`LOCATION=320`、`MAJOR=24`，且`normalization_aliases`、`location_hierarchies`当前均应全部为`active`
17. **2026-05-18 标准化词典回滚路径**：本次云端同步前备份已落在`/opt/offer360/deploy/prod/runtime/manual-sync-backups/normalization-before-*.sql`；后续若再同步该模块，必须先备份再导入，并继续使用`mysqldump --complete-insert --replace`避免跨环境字段顺序风险
18. **2026-05-18 求职服务页文案规则**：`/services`首屏第一屏文案必须站在用户价值与营销表达角度书写，禁止出现“页面数据来自商品库”“前后端同步”“已上架几款商品”这类研发/后台视角描述；当前定稿主标题为“求职这条路，不该只靠你一个人硬扛”，且首行“求职服务”标签已删除，主标题颜色改为主题色`text-brand`，副文案强调从简历精修到求职陪跑的全链路陪伴感与结果导向
19. **2026-06-03 标准发布上线记忆**：本次最新代码已按`docs/自动化标准部署手册.md`执行标准生产发布，发布标签为`20260603-024919`；本地通过`deploy/prod/scripts/package-release.sh`生成离线包`dist/offer360-offline-20260603-024919.tar`，上传至服务器`/opt/offer360`后，再由`/opt/offer360/deploy/prod/scripts/deploy-release.sh`完成导入镜像、数据库备份、结构校验、业务容器重建与验收。当前生产`release-state.env`已切换为`CURRENT_APP_IMAGE_TAG=\"20260603-024919\"`、`PREVIOUS_APP_IMAGE_TAG=\"20260603-015341\"`，`api`、`web`、`wechat-pay-gateway`三类业务容器均已运行在新标签，`mysql`、`redis`、`elasticsearch`保持原地健康运行；部署日志已确认本地入口、公网入口静态资源校验以及`robots.txt`/`sitemap.xml` SEO 验收全部通过

---

> 本档案为offer360项目唯一官方交接文档，后续开发前必须完整阅读并理解所有约束，确保开发一致性。
