## offer360 后台管理页面整体方案

### 一、结论先说

基于当前数据库里**已有的 15 张业务表**，结合现有前后端业务链路，我建议把后台按**7 大业务模块、16 个业务管理页面**来建设，这是一套能真正支撑运营、客服、财务、内容维护的**最小可运营版后台**。

如果按“正式上线可控”的标准来做，还应额外补上：

- **4 个系统管理页面**：后台登录、管理员与角色、菜单/接口权限、操作日志
- **2 个兑换码页面**：兑换码批次管理、兑换码明细管理

所以：

- **基于现有表的最小业务后台**：**16 个页面**
- **正式上线后台（含系统管理）**：**20 个页面**
- **增强版后台（再加兑换码能力）**：**22 个页面**

### 二、现状评估

#### 1. 当前库里已有的 15 张业务表

- `job_announcements`
- `users`
- `user_profiles`
- `user_job_preference_tags`
- `user_memberships`
- `membership_rich_text_contents`
- `service_products`
- `service_orders`
- `user_job_tracking`
- `inv_redirect_link`
- `inv_visitor_trace`
- `user_wallet`
- `commission_log`
- `commission_config`
- `inv_bind_log`

#### 2. 目前业务特征

这套库不是单纯内容站，而是一个明显的**“招聘信息 + 用户成长 + 会员变现 + 服务订单 + 邀请分销”**平台，因此后台不能只做基础表 CRUD，必须同时覆盖：

- **运营维护**：招聘公告、商品、会员权益内容
- **客服处理**：用户、会员、求职进度、订单
- **财务核对**：钱包、分销流水、订单导出
- **增长追踪**：邀请链接、访客追踪、绑定日志

#### 3. 当前后台建设缺口

当前仓库里只有 `web` 和 `api`，**没有独立后台管理端**，也没有后台账号/角色/权限/操作日志相关表。

这意味着：

- 业务后台页面可以立刻规划并分期建设；
- 但如果要正式上线给团队使用，必须补齐**后台系统能力**，否则无法安全交付。

### 三、后台模块结构与页面数量

#### 方案 A：最小业务后台（16 页）

| 模块 | 页面数 | 页面名称 |
|---|---:|---|
| 工作台 | 1 | 数据总览 |
| 招聘管理 | 1 | 招聘公告管理 |
| 用户运营 | 4 | 用户管理、求职意向管理、求职跟踪管理、邀请关系管理 |
| 会员管理 | 2 | 会员管理、会员权益内容管理 |
| 商品订单 | 2 | 服务商品管理、服务订单管理 |
| 邀请分销 | 5 | 邀请链接管理、邀请访问追踪、邀请绑定日志、用户钱包管理、分销流水管理 |
| 平台配置 | 1 | 分销配置 |
| **合计** | **16** |  |

#### 方案 B：正式上线后台（20 页）

在上面 16 页基础上，新增：

- 后台登录
- 管理员与角色管理
- 菜单/接口权限管理
- 操作日志

#### 方案 C：增强版后台（22 页）

在 20 页基础上，再新增：

- 兑换码批次管理
- 兑换码明细管理

> 说明：**兑换码相关表当前并不存在于现有数据库**，因此这 2 页属于建议扩展，不计入“基于现有表”的最小页数，但很适合后续会员商业化升级。

### 四、后台通用设计标准

所有后台页面建议统一遵循以下规范，避免后期页面风格和交互割裂。

#### 1. 常规主数据页面标准能力

适用于：招聘公告、商品、会员内容、用户主数据等页面。

- **列表区**：关键词查询、条件筛选、状态筛选、时间筛选、分页、排序
- **操作区**：新增、编辑、删除、查看详情、启用/停用、批量操作
- **详情区**：右侧抽屉或详情页，展示完整字段和关联数据
- **分页规则**：默认每页 20 条，可切换 20/50/100
- **导出规则**：导出当前筛选结果，不允许无条件导出全库大表

#### 2. 特殊业务页面标准能力

适用于：订单、钱包、分销流水、邀请追踪、绑定日志等页面。

- **以查询、导出、审计为主**
- 默认**不开放物理删除**
- 重要业务数据不允许直接修改关键金额、流水、关系链
- 允许增加“备注”“人工处理结果”“纠错记录”等辅助字段

#### 3. 导入导出统一标准

适用于你明确要求支持导入导出的页面。

- **导入前必须提供 Excel 模板下载**
- **模板内必须给字段说明、示例值、枚举值说明**
- **上传后先做预校验，再执行入库**
- **导入结果需输出成功/失败明细**
- **失败数据支持导出错误报告**
- **导出支持按筛选条件导出 Excel**

#### 4. 状态管理统一标准

- 有 `status` 字段的表，直接做启用/停用
- 没有 `status` 字段的表，优先做“业务状态展示”；若后台确实需要上下架/禁用，建议补字段
- 对订单、流水、绑定日志这类数据，**状态可展示，不建议人为随意篡改**

### 五、16 个业务后台页面的完整设计方案

下面按“页面目标 + 关联表 + 页面设计 + 特殊能力 + 落地建议”展开。

---

## 1. 数据总览

- **页面定位**：后台首页，给老板、运营、客服、财务看整体经营数据
- **关联表**：`job_announcements`、`users`、`user_memberships`、`service_orders`、`service_products`、`user_wallet`、`commission_log`
- **核心模块**：
  - 今日/近 7 日/近 30 日新增岗位数
  - 累计用户数、近 7 日活跃登录数
  - 当前有效会员数、近 30 日新增会员数
  - 服务订单数、支付金额、退款金额（如后续有退款）
  - 分销累计发放金额、钱包余额总额
  - 热门服务 TOP 榜、热门招聘企业 TOP 榜
- **页面形式**：统计卡片 + 趋势图 + 榜单 + 待办提醒
- **特殊能力**：支持时间维度切换、导出总览报表
- **落地建议**：优先级 P0，作为后台第一屏必须有

## 2. 招聘公告管理

- **页面定位**：运营维护招聘公告的核心页面
- **关联表**：`job_announcements`
- **常规功能**：
  - 新增、编辑、删除、查询、分页
  - 按公司名、岗位名、地点、学历、企业性质、招聘类型、截止时间筛选
  - 支持按更新时间排序、截止时间排序
- **列表字段建议**：
  - 公司名称
  - 岗位类别
  - 岗位名称
  - 工作地点
  - 学历要求
  - 企业性质
  - 招聘类型
  - 截止日期
  - 更新时间
- **编辑表单建议**：
  - 公司名称、岗位类别、岗位名称、工作地点
  - 学历要求、企业性质、招聘类型、专业需求
  - 截止时间、公告链接、投递链接、投递方式、内推码
- **特殊能力**：
  - **批量导入**
  - **批量导出**
  - **Excel 模板下载**
  - 批量修改截止时间
  - 批量修改招聘类型
  - 批量删除/归档
- **导入模板字段**：
  - 公司名称、岗位类别、岗位名称、工作地点、学历要求、企业性质、招聘类型、专业需求、截止时间、公告链接、投递链接、投递方式、内推码
- **重要建议**：
  - 当前表**没有发布状态字段**，若后台需要“草稿/发布/下线”，建议新增 `status`
- **优先级**：P0

## 3. 用户管理

- **页面定位**：客服和运营查看、维护用户主档
- **关联表**：`users`、`user_profiles`、`user_memberships`、`user_wallet`
- **常规功能**：
  - 新增、编辑、删除、查询、分页
  - 按手机号、邀请码、邀请人、注册时间、最近登录时间筛选
- **列表字段建议**：
  - 用户 ID
  - 手机号
  - 邀请码
  - 上级邀请人
  - 注册时间
  - 最近登录时间
  - 会员状态
  - 钱包余额
- **详情页建议采用 Tab**：
  - 基础信息
  - 个人资料
  - 求职意向
  - 会员信息
  - 钱包与分销
  - 订单记录
  - 求职进度
- **特殊能力**：
  - **批量导入**
  - **批量导出**
  - **Excel 模板下载**
  - 批量开通会员/延长会员
  - 批量绑定或修正邀请关系（需二次确认）
- **用户导入模板建议字段**：
  - 手机号、初始密码、姓名、毕业年份、学历、学校、专业、意向城市、意向岗位、意向公司、上级邀请码、会员到期时间
- **重要建议**：
  - 当前 `users` 表**没有账号状态字段**，若后台要支持封禁/启用，建议新增 `status`
- **优先级**：P0

## 4. 求职意向管理

- **页面定位**：集中查看用户画像，支撑推荐、运营分析
- **关联表**：`user_job_preference_tags`
- **常规功能**：
  - 查询、分页、编辑、删除
  - 按用户手机号、意向城市、意向岗位、意向公司筛选
- **列表字段建议**：
  - 用户手机号
  - 意向城市
  - 意向岗位
  - 意向公司
  - 创建时间
  - 更新时间
- **特殊能力**：
  - 支持标签展开查看
  - 支持导出用户画像数据
- **落地说明**：
  - 这页不一定要做“新增”，因为多数意向数据来源于用户前台填写，但后台仍应保留人工补录能力
- **优先级**：P1

## 5. 求职跟踪管理

- **页面定位**：客服排查用户投递进展、用户运营分析
- **关联表**：`user_job_tracking`、`users`、`job_announcements`
- **常规功能**：
  - 查询、分页、编辑、删除
  - 按用户、公司、岗位、进度状态、更新时间筛选
- **列表字段建议**：
  - 用户手机号
  - 公司名称
  - 岗位名称
  - 当前进度
  - 创建时间
  - 更新时间
- **特殊能力**：
  - 批量更新进度
  - 导出用户求职进展表
  - 查看关联岗位详情与用户画像
- **状态建议**：
  - 进度值统一为枚举，后台只允许选择，不允许自由输入
- **优先级**：P1

## 6. 邀请关系管理

- **页面定位**：运营查看谁邀请了谁，处理关系链纠错
- **关联表**：`users`（重点使用 `parent_uid`）
- **常规功能**：
  - 查询、分页
  - 按邀请人手机号、被邀请人手机号、邀请码筛选
- **列表字段建议**：
  - 邀请人
  - 被邀请人
  - 邀请人邀请码
  - 绑定时间（可结合注册时间/日志）
  - 当前会员状态
  - 累计消费金额
- **特殊能力**：
  - 人工修正邀请关系（必须二次确认并记录原因）
  - 查看邀请裂变树
  - 导出邀请关系报表
- **重要说明**：
  - 这页本质是增长/结算敏感页，不建议开放物理删除
- **优先级**：P1

## 7. 会员管理

- **页面定位**：客服和运营管理会员有效期与状态
- **关联表**：`user_memberships`、`users`
- **常规功能**：
  - 新增、编辑、删除、查询、分页
  - 按手机号、会员状态、到期时间筛选
- **列表字段建议**：
  - 用户手机号
  - 开通时间
  - 到期时间
  - 剩余天数
  - 当前状态（有效/已过期）
- **特殊能力**：
  - 手动开通会员
  - 延长会员时长
  - 批量赠送会员天数
  - 到期预警导出
- **后台操作建议**：
  - 提供快捷按钮：赠送 30 天 / 90 天 / 180 天 / 365 天
- **优先级**：P0

## 8. 会员权益内容管理

- **页面定位**：管理会员开通页底部富文本权益内容
- **关联表**：`membership_rich_text_contents`
- **常规功能**：
  - 新增、编辑、删除、查询
  - 按 `slug`、标题查询
- **编辑区建议**：
  - 标题
  - `slug`
  - 富文本 HTML 内容
  - 实时预览区
- **特殊能力**：
  - 富文本编辑器
  - 预览发布
  - 历史版本（建议补）
- **重要建议**：
  - 当前表缺少发布状态与版本号，若多人维护内容，建议新增：`status`、`version`、`published_at`
- **优先级**：P0

## 9. 服务商品管理

- **页面定位**：运营维护求职服务商品
- **关联表**：`service_products`
- **常规功能**：
  - 新增、编辑、删除、查询、分页
  - 启用/停用
  - 按服务名称、热销状态、上架状态筛选
- **列表字段建议**：
  - 服务名称
  - 现价
  - 原价
  - 评分
  - 销量
  - 热销标记
  - 上架状态
  - 更新时间
- **编辑表单建议**：
  - 名称、描述、价格、原价、评分、热销标记、状态
- **特殊能力**：
  - 批量上架/下架
  - 批量设置热销
  - 导出商品清单
- **优先级**：P0

## 10. 服务订单管理

- **页面定位**：财务、客服处理订单查询与售后
- **关联表**：`service_orders`、`users`、`service_products`
- **常规功能**：
  - 查询、分页、详情
  - 按订单号、用户手机号、商品名、支付状态、支付时间筛选
- **列表字段建议**：
  - 订单号
  - 用户
  - 商品名称
  - 金额
  - 支付状态
  - 支付时间
  - 创建时间
- **特殊能力**：
  - **批量导出**
  - 财务对账导出
  - 人工标记异常订单
  - 订单备注
- **重要说明**：
  - 订单不建议开放物理删除
  - 若后续要做退款，应新增退款操作流与审批记录
- **优先级**：P0

## 11. 邀请链接管理

- **页面定位**：运营管理邀请短链、中转链接有效性
- **关联表**：`inv_redirect_link`
- **常规功能**：
  - 新增、编辑、删除、查询、分页
  - 按邀请人、随机 key、过期状态筛选
- **列表字段建议**：
  - 邀请人
  - 随机 key
  - 对外链接
  - 过期时间
  - 创建时间
  - 当前状态（有效/已过期）
- **特殊能力**：
  - 重新生成链接
  - 复制分享链接
  - 批量失效处理
- **状态说明**：
  - 可通过 `expire_at` 自动计算状态，不一定强制补 `status`
- **优先级**：P1

## 12. 邀请访问追踪

- **页面定位**：增长分析 + 风控排查
- **关联表**：`inv_visitor_trace`
- **常规功能**：
  - 查询、分页
  - 按邀请人、IP、UserAgent、点击时间、过期时间筛选
- **列表字段建议**：
  - 追踪号
  - 邀请人
  - IP
  - 设备信息
  - 点击时间
  - 过期时间
- **特殊能力**：
  - 风险 IP 检索
  - 同设备多次访问识别
  - 导出访问轨迹
- **重要说明**：
  - 此页属于日志审计页，不建议开放编辑/删除
- **优先级**：P2

## 13. 邀请绑定日志

- **页面定位**：排查邀请关系绑定问题、结算追责
- **关联表**：`inv_bind_log`
- **常规功能**：
  - 查询、分页
  - 按邀请人、被邀请人、绑定时间筛选
- **列表字段建议**：
  - 邀请人
  - 被邀请人
  - 绑定时间
- **特殊能力**：
  - 导出绑定日志
  - 跳转到邀请关系详情
- **重要说明**：
  - 日志类数据不开放新增/编辑/删除
- **优先级**：P2

## 14. 用户钱包管理

- **页面定位**：财务查看用户钱包余额和累计收益
- **关联表**：`user_wallet`、`users`
- **常规功能**：
  - 查询、分页、详情
  - 按手机号、余额区间、累计收益筛选
- **列表字段建议**：
  - 用户手机号
  - 可用余额
  - 冻结余额
  - 累计收益
  - 更新时间
- **特殊能力**：
  - 导出钱包报表
  - 查看钱包关联流水
  - 人工冻结/解冻（若后续支持提现）
- **重要说明**：
  - 余额类数据不建议直接手工改值，应通过“资金调整单”实现；若业务需要，建议新增调整记录表
- **优先级**：P1

## 15. 分销流水管理

- **页面定位**：财务对账与分销收益核算核心页
- **关联表**：`commission_log`、`service_orders`、`users`
- **常规功能**：
  - 查询、分页、详情
  - 按订单号、邀请人、消费人、流水类型、时间筛选
- **列表字段建议**：
  - 订单号
  - 邀请人
  - 消费人
  - 提成比例
  - 提成金额
  - 原始消费金额
  - 流水类型
  - 生成时间
- **特殊能力**：
  - **批量导出**
  - 财务对账导出
  - 区分正常提成 / 退款冲减
- **重要说明**：
  - 分销流水必须只读，不开放人工删除/修改
- **优先级**：P0

## 16. 分销配置

- **页面定位**：平台级单例配置页
- **关联表**：`commission_config`
- **页面形式**：单页表单，不做复杂列表
- **核心字段**：
  - 一级分销比例
  - 更新时间
- **特殊能力**：
  - 配置变更二次确认
  - 保存前风险提示
  - 生效说明（新订单生效）
- **重要说明**：
  - 这是典型的“单例配置页”，不建议做删除
- **优先级**：P0

### 六、你特别提到的特殊场景方案

#### 1. 招聘公告：必须支持导入 / 导出 / 模板下载

建议做成以下完整流程：

- 下载模板
- 按模板填写
- 上传 Excel
- 系统预校验
- 返回校验报告
- 确认导入
- 输出导入结果
- 支持导出当前筛选结果

建议增加：

- 导入任务记录
- 导入失败原因下载
- 重复公司 + 岗位 + 截止日期的去重校验

#### 2. 用户管理：必须支持导入 / 导出 / 模板下载

用户导入不能只写 `users` 一张表，建议后台导入时支持联动写入：

- `users`
- `user_profiles`
- `user_job_preference_tags`
- 可选写入 `user_memberships`

这类导入最适合用于：

- 历史数据迁移
- 活动白名单导入
- 运营批量补录用户

#### 3. 兑换码：建议作为独立增强模块

当前数据库**没有兑换码表**，但从业务上看，这块很值得补。

建议新增 2 张核心表：

- `membership_redeem_code_batches`
- `membership_redeem_codes`

如需追踪核销明细，再补：

- `membership_redeem_use_logs`

对应后台建议 2 个页面：

##### 兑换码批次管理

- 选择卡类型：月卡、季卡、半年卡、年卡
- 输入生成数量
- 设置有效期、备注、是否启用
- 一键批量生成
- 导出本批次兑换码

##### 兑换码明细管理

- 按兑换码、批次、状态查询
- 状态：未使用、已使用、已过期、已作废
- 支持作废、导出、查看使用人和使用时间

### 七、建议补充的字段与表

如果要让后台真正顺手，以下补充非常有必要。

#### 1. 建议补字段

- `job_announcements`：增加 `status`、`source_channel`、`remark`
- `users`：增加 `status`、`source_type`
- `membership_rich_text_contents`：增加 `status`、`version`、`published_at`
- `service_orders`：增加 `refund_reason`、`refund_at`（如做退款）

#### 2. 建议补系统表

- `admin_users`
- `admin_roles`
- `admin_role_permissions`
- `admin_operation_logs`

#### 3. 建议补任务表（可选）

如果导入导出数据量较大，建议补：

- `import_tasks`
- `export_tasks`

用于异步任务、失败重试、结果文件下载。

### 八、实施优先级建议

#### 第一阶段（必须先做，P0）

- 数据总览
- 招聘公告管理
- 用户管理
- 会员管理
- 会员权益内容管理
- 服务商品管理
- 服务订单管理
- 分销流水管理
- 分销配置

#### 第二阶段（业务运营增强，P1）

- 求职意向管理
- 求职跟踪管理
- 邀请关系管理
- 邀请链接管理
- 用户钱包管理

#### 第三阶段（审计与风控增强，P2）

- 邀请访问追踪
- 邀请绑定日志
- 兑换码模块（如启动会员活动）

### 九、最终建议

如果你的目标是：

- **先把后台尽快做起来，支持团队日常运营**，就按 **16 个业务页面** 开工；
- **要给多人长期使用并正式上线**，建议直接按 **20 个页面** 建设，并把权限与操作日志一起做掉；
- **后续要做会员促销、拉新活动、渠道投放**，建议同步补上**兑换码模块**，形成 **22 页增强版后台**。

我对这套项目的最终判断是：

- **最合理的第一期后台规模：16 页**
- **最合理的正式上线规模：20 页**
- **最有商业扩展性的完整规模：22 页**

如果你需要，我下一步可以继续直接给你输出一版：

1. **后台左侧菜单树 + 页面路由命名方案**
2. **每个页面的字段清单 / 筛选项 / 按钮文案 / 弹窗表单设计**
3. **后台数据库补表方案（管理员、权限、兑换码、导入任务）**
4. **后台产品原型结构图**
